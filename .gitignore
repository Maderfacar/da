# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist
test.html
.stylelintcache
test.html

# Node dependencies
node_modules

# TinyMCE self-hosted assets (auto-copied via postinstall)
public/tinymce

# Playwright MCP test artifacts
.playwright-mcp/
/*.png

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

.stylelintcache
# Lock files（僅保留 pnpm-lock.yaml，npm 與 yarn 的不 commit）
package-lock.json
yarn.lock

# Local env files
.env
.env.dev

# Firebase service account keys（敏感憑證，不 commit）
docs/*.json
*adminsdk*.json
.vercel

# 業務規格文件僅保留本地，不部署到 GitHub（含 prd / tasks / decision-log 等）
docs/

# Playwright e2e 執行紀錄
test-results/

# Git worktrees（由 Claude Code 管理，不 commit）
.claude/worktrees/
