# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist
test.html
.stylelintcache
test.html

# Node dependencies
node_modules

# TinyMCE self-hosted assets (auto-copied via postinstall)
public/tinymce

# Playwright MCP test artifacts
.playwright-mcp/
/*.png

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

.stylelintcache
# Lock files（僅保留 pnpm-lock.yaml，npm 與 yarn 的不 commit）
package-lock.json
yarn.lock

# Local env files
.env
.env.dev

# Firebase service account keys（敏感憑證，不 commit）
docs/*.json
*adminsdk*.json
.vercel

# Git worktrees（由 Claude Code 管理，不 commit）
.claude/worktrees/
